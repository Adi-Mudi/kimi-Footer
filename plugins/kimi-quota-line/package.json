{
	"name": "kimi-quota-line",
	"version": "1.3.2",
	"description": "Kimi Code CLI status line plugin — sunset folder + Kimi/MiniMax quota bars (kimi-native)",
	"type": "module",
	"license": "MIT",
	"keywords": [
		"kimi-code",
		"kimi",
		"status-line",
		"quota",
		"minimax",
		"footer"
	],
	"scripts": {
		"test": "vitest run --exclude '**/stress.test.ts'",
		"test:watch": "vitest --exclude '**/stress.test.ts'",
		"stress": "vitest run test/stress.test.ts",
		"e2e": "npm run typecheck && npm test && npm run stress && npm run build && echo '--- E2E SUBPROCESS SMOKE ---' && echo '{\"model\":\"kimi-for-coding\",\"cwd\":\"/tmp\"}' | XDG_RUNTIME_DIR=/tmp KIMI_API_KEY=fake KIMI_CODE_HOME=/tmp node ./dist/bin/render-row1.js",
		"build": "tsc -p tsconfig.build.json",
		"build:watch": "tsc --watch --noEmit",
		"sync": "node ./tools/sync-to-managed.mjs",
		"postinstall": "node ./tools/postinstall.mjs",
		"typecheck": "tsc --noEmit",
		"lint": "biome lint",
		"lint:fix": "biome lint --write",
		"format": "biome format --write",
		"check": "biome check"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.14",
		"@types/node": "^22.0.0",
		"typescript": "^5.6.0",
		"vitest": "^2.1.0"
	}
}
